#!/bin/bash
# Post-removal script for open-sesame
set -e

case "$1" in
    remove|purge)
        echo "open-sesame removed."
        echo "User configuration remains at ~/.config/pds/"
        echo "To remove: rm -rf ~/.config/pds/"
        #DEBHELPER#
        ;;
esac

exit 0
