#!/bin/bash
# Post-removal script for open-sesame
set -e

case "$1" in
    remove|purge)
        echo "open-sesame removed."
        echo "User configuration remains at ~/.config/open-sesame/"
        echo "To remove: rm -rf ~/.config/open-sesame/"
        #DEBHELPER#
        ;;
esac

exit 0
